Full session handoff for next-day pickup. More thorough than checkpoint.
1. Run full checkpoint process.
2. Update docs/preferences.md if user expressed new preferences.
3. Write comprehensive status/handoff.md: project context, task + progress %, ALL files modified + purpose, ALL decisions, style/theme/tone in use, exact next steps, blockers.
4. Run `git status` and `git log --oneline -5` for version control state.
5. Confirm: "Full handoff saved. Next session has complete context."
CRITICAL: Must be detailed enough for a fresh session to continue identically.