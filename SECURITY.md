# Security Policy
Report vulnerabilities via email, not public issues.
ClaudeForge: no network at runtime, no code execution, no secrets, backup before modify, manifest tracking.
Inspect before running: curl -sL [url]/install.sh > install.sh && less install.sh && bash install.sh
